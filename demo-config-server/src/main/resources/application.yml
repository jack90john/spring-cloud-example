spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: git-repo-url
          searchPaths: searchPaths
          username: username
          password: password
server:
  port: 9000